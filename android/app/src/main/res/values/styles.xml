<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:datePickerDialogTheme" tools:targetApi="lollipop">@style/DialogDatePicker.Theme</item>
        <item name="android:timePickerDialogTheme" tools:targetApi="lollipop">@style/DialogPicker.Theme</item>
    </style>
    <style name="SplashScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <style name="DialogDatePicker.Theme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/light_red</item>
        <item name="android:textColor">@color/dove_gray</item>
        <item name="android:textColorPrimary">@color/dove_gray</item>
    </style>
    <style name="DialogPicker.Theme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/light_red</item>
        <item name="android:textColorPrimary">@color/dove_gray</item>
    </style>

</resources>
